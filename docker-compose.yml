version: '3.9'
services:
  frontend:
    build: ./frontend-vue
    ports:
      - 3000:3000
    platform: linux/x86_64
    working_dir: /app-frontend
  
  backend:
    build: ./backend-vue
    platform: linux/x86_64
    working_dir: /app-backend